name: CICD
on: 
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: first_joba
        run: echo "Hello, world!"
      - name: second_joba
        run: |
          echo "Hello, world!"
          echo "Hello, world!"
